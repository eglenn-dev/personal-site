{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "dev emails",
            "type": "shell",
            "command": "bun install; bun run dev",
            "options": {
                "cwd": "${workspaceFolder}/react-email"
            }
        },
        {
            "label": "start dev",
            "type": "shell",
            "command": "bun install; bun run dev",
        },
    ]
}